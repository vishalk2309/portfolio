# Copy this file to .env and fill in your values.
# Find them in Supabase → Project Settings → API.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# The email allowed into /admin. Must match your Supabase Auth admin user and
# the email in buyer-accounts.sql's is_owner(). Optional — defaults in code.
VITE_OWNER_EMAIL=kushwahavishal296@gmail.com
