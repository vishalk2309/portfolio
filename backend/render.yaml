services:
  - type: web
    name: portfolio-ai-backend
    runtime: docker
    region: oregon
    dockerCommand: java -jar target/portfolio-ai-backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: "8080"
